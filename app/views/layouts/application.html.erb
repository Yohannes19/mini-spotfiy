<!DOCTYPE html>
<html>
<head>
  <title>MiniSpotify</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
 
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

  <!-- Bootstrap 5 CDN -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-success mb-4">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">🎵 MiniSpotify</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto">
        <li class="nav-item"><%= link_to 'Artists', artists_path, class: 'nav-link' %></li>
        <li class="nav-item"><%= link_to 'Albums', albums_path, class: 'nav-link' %></li>
        <li class="nav-item"><%= link_to 'Tracks', tracks_path, class: 'nav-link' %></li>
        <li class="nav-item"><%= link_to 'My Playlists', playlists_path, class: "nav-link" %></li>
      </ul>

      <% if user_signed_in? %>
        <span class="navbar-text me-3">Hi, <%= current_user.email %></span>
        <%= button_to 'Logout', destroy_user_session_path, method: :delete, class: 'btn btn-outline-light btn-sm' %>
      <% else %>
        <%= link_to 'Sign Up', new_user_registration_path, class: 'btn btn-outline-light btn-sm me-2' %>
        <%= link_to 'Login', new_user_session_path, class: 'btn btn-light btn-sm' %>
      <% end %>
    </div>
  </div>
</nav>

<div class="container">
  <%= yield %>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
